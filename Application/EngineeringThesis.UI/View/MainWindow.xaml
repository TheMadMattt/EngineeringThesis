<Window x:Class="EngineeringThesis.UI.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EngineeringThesis.UI.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="1000" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}" x:Key="CenterAlignment">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="{x:Type TextBlock}" x:Key="WrapCenterColumn">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </Window.Resources>
    <Grid>
        <DataGrid Name="DataGrid" Height="300px" AutoGenerateColumns="False" VerticalAlignment="Top" 
                  IsReadOnly="True" MouseDoubleClick="DataGrid_MouseDoubleClick" ScrollViewer.CanContentScroll="False" 
                  HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numer faktury" Binding="{Binding InvoiceNumber}" 
                                    ElementStyle="{StaticResource CenterAlignment}" Width="Auto"/>
                <DataGridTextColumn Header="Data wystawienia" Binding="{Binding InvoiceDate, StringFormat=d, ConverterCulture=pl-PL}"  
                                    ElementStyle="{StaticResource CenterAlignment}" Width="Auto"/>
                <DataGridTextColumn Header="Nazwa firmy" Binding="{Binding Contractor.Name}" 
                                    ElementStyle="{StaticResource WrapCenterColumn}" Width="*"/>
                <DataGridTemplateColumn Header="Adres firmy" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" TextWrapping="Wrap">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} {1} &#x0a;{2} {3}">
                                        <Binding Path="Contractor.ZipCode" />
                                        <Binding Path="Contractor.City" />
                                        <Binding Path="Contractor.Street"></Binding>
                                        <Binding Path="Contractor.StreetNumber"></Binding>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Sprzedawca" Binding="{Binding Seller.Name}" 
                                    ElementStyle="{StaticResource WrapCenterColumn}" Width="*"/>
                <DataGridTextColumn Header="Data płatności" Binding="{Binding PaymentDeadline, StringFormat=d, ConverterCulture=pl-PL}" 
                                    ElementStyle="{StaticResource CenterAlignment}" Width="Auto"/>
                <DataGridTextColumn Header="Uwagi" Binding="{Binding Comments}" ElementStyle="{StaticResource WrapCenterColumn}" Width="2*">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
